/* A program to perform Euclid’s algorithm to
compute the greatest common divisor */

/* function call to gcd is missing parameters */
/* Comment included in the middle */

int gcd(int u, int v) {
    if (v == 0) 
        /* return u comment */
        return u;
    else 
        return gcd(v, u - u/v*v);
}

void main(void) {
    int x;
    int y;
    x = input(); y = input();
    output(gcd());
}